using BOSLib;
using System;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region ARReminds
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:ARRemindsController
    //Created Date:Thursday, April 5, 2018
    //-----------------------------------------------------------

    public class ARRemindsController : BaseBusinessController
    {
        public ARRemindsController()
        {
            dal = new DALBaseProvider("ARReminds", typeof(ARRemindsInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<ARRemindsInfo> remindsList = new List<ARRemindsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ARRemindsInfo objRemindsInfo = (ARRemindsInfo)GetObjectFromDataRow(row);
                    remindsList.Add(objRemindsInfo);
                }
            }
            ds.Dispose();
            return remindsList;
        }

        public List<ARRemindsInfo> GetAllRemindsForHome(DateTime? remindFromDate, DateTime? remindToDate)
        {
            DataSet results = dal.GetDataSet("ARReminds_GetAllRemindsForHome", remindFromDate, remindToDate);
            return (List<ARRemindsInfo>)GetListFromDataSet(results);
        }
    }
    #endregion
}