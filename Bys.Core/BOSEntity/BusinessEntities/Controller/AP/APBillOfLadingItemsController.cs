using BOSLib;
using System;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region APBillOfLadingItems
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:APBillOfLadingItemsController
    //Created Date:Thursday, January 11, 2018
    //-----------------------------------------------------------

    public class APBillOfLadingItemsController : BaseBusinessController
    {
        public APBillOfLadingItemsController()
        {
            dal = new DALBaseProvider("APBillOfLadingItems", typeof(APBillOfLadingItemsInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<APBillOfLadingItemsInfo> results = new List<APBillOfLadingItemsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    APBillOfLadingItemsInfo obj = (APBillOfLadingItemsInfo)GetObjectFromDataRow(row);
                    results.Add(obj);
                }
            }
            ds.Dispose();
            return results;
        }

        public List<APBillOfLadingItemsInfo> GetBOLForExportExcel(DateTime fromDate, DateTime toDate, int objectID, string objectType, string contractNo, string viewType)
        {
            DataSet ds = dal.GetDataSet("APBillOfLadingItems_GetBOLForExportExcel", fromDate, toDate, objectID, objectType, contractNo, viewType);
            List<APBillOfLadingItemsInfo> invoices = new List<APBillOfLadingItemsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    APBillOfLadingItemsInfo objInvoiceInsInfo = (APBillOfLadingItemsInfo)GetObjectFromDataRow(row);
                    invoices.Add(objInvoiceInsInfo);

                }
            }
            return invoices;
        }

        public List<APBillOfLadingItemsInfo> GetItemByInvoiceInID(int invoiceInID)
        {
            DataSet ds = dal.GetDataSet("APBillOfLadingItems_GetItemByInvoiceInID", invoiceInID);
            return (List<APBillOfLadingItemsInfo>)GetListFromDataSet(ds);
        }
    }
    #endregion
}