using BOSLib;
using System;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GEAccountingFormulaItems
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GEAccountingFormulaItemsController
    //Created Date:04 Tháng Mười 2016
    //-----------------------------------------------------------

    public class GEAccountingFormulaItemsController : BaseBusinessController
    {
        public GEAccountingFormulaItemsController()
        {
            dal = new DALBaseProvider("GEAccountingFormulaItems", typeof(GEAccountingFormulaItemsInfo));
        }

        public List<GEAccountingFormulaItemsInfo> GetObjectForReport(string report)
        {
            DataSet ds = dal.GetDataSet("GEAccountingFormulaItems_GetObjectForReport", report);
            List<GEAccountingFormulaItemsInfo> AccountingFormulaItemList = new List<GEAccountingFormulaItemsInfo>();
            if (ds.Tables.Count > 0)
            {//'"P
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEAccountingFormulaItemsInfo objAccountingFormulaItemsInfo = (GEAccountingFormulaItemsInfo)GetObjectFromDataRow(row);
                    AccountingFormulaItemList.Add(objAccountingFormulaItemsInfo);
                }
            }
            return AccountingFormulaItemList;
        }

        public List<GEAccountingFormulaItemsInfo> GetObjectForReportByFormulaType(string formulaType)
        {
            DataSet ds = dal.GetDataSet("GEAccountingFormulaItems_GetObjectForReportByFormulaType", formulaType);
            List<GEAccountingFormulaItemsInfo> AccountingFormulaItemList = new List<GEAccountingFormulaItemsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEAccountingFormulaItemsInfo objAccountingFormulaItemsInfo = (GEAccountingFormulaItemsInfo)GetObjectFromDataRow(row);
                    AccountingFormulaItemList.Add(objAccountingFormulaItemsInfo);
                }
            }
            return AccountingFormulaItemList;
        }

        public List<GEAccountingFormulaItemsInfo> GetAccountingFormulaItemLCTTTTs(DateTime? fromDate
                                                                            , DateTime? toDate
                                                                            , DateTime? openingFromDate
                                                                            , DateTime? openingToDate
                                                                            , int? branchID
                                                                            , int? branchCentralID)
        {
            DataSet ds = dal.GetDataSet("GEAccountingFormulaItems_GetAccountingFormulaItemLCTTTTs", fromDate
                                       , toDate
                                       , openingFromDate
                                       , openingToDate
                                       , branchID
                                       , branchCentralID);
            List<GEAccountingFormulaItemsInfo> results = new List<GEAccountingFormulaItemsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEAccountingFormulaItemsInfo objAccountingFormulaItemsInfo = (GEAccountingFormulaItemsInfo)GetObjectFromDataRow(row);
                    results.Add(objAccountingFormulaItemsInfo);
                }
            }
            return results;
        }
    }
    #endregion
}