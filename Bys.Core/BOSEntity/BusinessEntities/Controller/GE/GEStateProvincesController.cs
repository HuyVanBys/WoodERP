using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GEStateProvinces
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GEStateProvincesController
    //Created Date:22 October 2013
    //-----------------------------------------------------------

    public class GEStateProvincesController : BaseBusinessController
    {
        public GEStateProvincesController()
        {
            dal = new DALBaseProvider("GEStateProvinces", typeof(GEStateProvincesInfo));
        }
        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<GEStateProvincesInfo> stateProvinces = new List<GEStateProvincesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEStateProvincesInfo objStateProvincesInfo = (GEStateProvincesInfo)GetObjectFromDataRow(row);
                    stateProvinces.Add(objStateProvincesInfo);
                }
            }
            ds.Dispose();
            return stateProvinces;
        }
        public List<GEStateProvincesInfo> getAllAliveStateProvinces()
        {
            List<GEStateProvincesInfo> listGEStateProvince = new List<GEStateProvincesInfo>();
            DataSet ds = dal.GetDataSet("GEStateProvinces_GetAliveGEStateProvinces");
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEStateProvincesInfo obj = (GEStateProvincesInfo)GetObjectFromDataRow(row);
                    listGEStateProvince.Add(obj);
                }
            }
            return listGEStateProvince;
        }
    }


    #endregion
}