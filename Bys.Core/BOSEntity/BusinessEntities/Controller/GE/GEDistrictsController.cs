using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GEDistricts
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GEDistrictsController
    //Created Date:22 October 2013
    //-----------------------------------------------------------

    public class GEDistrictsController : BaseBusinessController
    {
        public GEDistrictsController()
        {
            dal = new DALBaseProvider("GEDistricts", typeof(GEDistrictsInfo));
        }
        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<GEDistrictsInfo> districts = new List<GEDistrictsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEDistrictsInfo objDistrictsInfo = (GEDistrictsInfo)GetObjectFromDataRow(row);
                    districts.Add(objDistrictsInfo);
                }
            }
            ds.Dispose();
            return districts;
        }
        public GEDistrictsInfo GetGEDistrictByNameAndParentID(string name, int parentID)
        {
            List<GEDistrictsInfo> listGEDistricts = new List<GEDistrictsInfo>();
            DataSet ds = dal.GetDataSet("GEDistricts_GetGEDistrictByNameAndParentID", name, parentID);
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEDistrictsInfo obj = (GEDistrictsInfo)GetObjectFromDataRow(row);
                    listGEDistricts.Add(obj);
                }
            }
            if (listGEDistricts.Count > 0) return listGEDistricts[0];
            else
                return null;
        }
        public List<GEDistrictsInfo> GetAllDistricts()
        {
            DataSet ds = GetAllObjects();
            return (List<GEDistrictsInfo>)GetListFromDataSet(ds);
        }
    }
    #endregion
}