using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GETablesController
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GEContainersController
    //Created Date:23 Tháng Chín 2016
    //-----------------------------------------------------------

    public class GETablesController : BaseBusinessController
    {
        public GETablesController()
        {
            dal = new DALBaseProvider("GETables", typeof(GETablesInfo));
        }

        public List<GETablesInfo> GetColumnNamesByTableName(string tableName)
        {
            DataSet ds = dal.GetDataSet("GetColumnNamesByTableName", tableName);
            List<GETablesInfo> countryList = new List<GETablesInfo>();
            if (ds.Tables[0].Rows.Count > 0)
            {
                foreach (DataRow dr in ds.Tables[0].Rows)
                {
                    GETablesInfo objCountrysInfo = new GETablesInfo();
                    objCountrysInfo = (GETablesInfo)this.GetObjectFromDataRow(dr);
                    countryList.Add(objCountrysInfo);
                }
            }
            return countryList;
        }
        public DataSet GetTableColumnNamesByTableName(string tableName)
        {
            DataSet ds = dal.GetDataSet("GetColumnNamesByTableName", tableName);
            return ds;
        }
        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<GETablesInfo> objList = new List<GETablesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GETablesInfo obj = (GETablesInfo)GetObjectFromDataRow(row);
                    objList.Add(obj);
                }
            }
            ds.Dispose();
            return objList;
        }
    }
    #endregion
}