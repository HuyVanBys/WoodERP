using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GECountrys
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GECountrysController
    //Created Date:22 October 2013
    //-----------------------------------------------------------

    public class GECountrysController : BaseBusinessController
    {
        public GECountrysController()
        {
            dal = new DALBaseProvider("GECountrys", typeof(GECountrysInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<GECountrysInfo> countries = new List<GECountrysInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GECountrysInfo objCountrysInfo = (GECountrysInfo)GetObjectFromDataRow(row);
                    countries.Add(objCountrysInfo);
                }
            }
            ds.Dispose();
            return countries;
        }

        public List<GECountrysInfo> GetAllCountry()
        {
            DataSet ds = dal.GetDataSet("GECountrys_GetAllCountry");
            List<GECountrysInfo> countryList = new List<GECountrysInfo>();
            if (ds.Tables[0].Rows.Count > 0)
            {
                GECountrysController objCountrysController = new GECountrysController();
                foreach (DataRow dr in ds.Tables[0].Rows)
                {
                    GECountrysInfo objCountrysInfo = new GECountrysInfo();
                    objCountrysInfo = (GECountrysInfo)objCountrysController.GetObjectFromDataRow(dr);
                    countryList.Add(objCountrysInfo);
                }
            }
            return countryList;
        }
        public List<GECountrysInfo> GetAllCountryAlive()
        {
            DataSet ds = dal.GetDataSet("GECountrys_GetAllCountryAlive");
            List<GECountrysInfo> countryList = new List<GECountrysInfo>();
            if (ds.Tables[0].Rows.Count > 0)
            {
                GECountrysController objCountrysController = new GECountrysController();
                foreach (DataRow dr in ds.Tables[0].Rows)
                {
                    GECountrysInfo objCountrysInfo = new GECountrysInfo();
                    objCountrysInfo = (GECountrysInfo)objCountrysController.GetObjectFromDataRow(dr);
                    countryList.Add(objCountrysInfo);
                }
            }
            return countryList;
        }
        public GECountrysInfo GetDefaultCountry()
        {
            DataSet ds = dal.GetDataSet("GECountrys_GetDefaultCountry");
            if (ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0)
            {
                GECountrysInfo objCountrysInfo = (GECountrysInfo)GetObjectFromDataRow(ds.Tables[0].Rows[0]);
                return objCountrysInfo;
            }
            return null;
        }    
    }
    #endregion
}