using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region GEReligions
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:GEReligionsController
    //Created Date:Monday, June 13, 2016
    //-----------------------------------------------------------

    public class GEReligionsController : BaseBusinessController
    {
        public GEReligionsController()
        {
            dal = new DALBaseProvider("GEReligions", typeof(GEReligionsInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<GEReligionsInfo> religions = new List<GEReligionsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    GEReligionsInfo objReligionsInfo = (GEReligionsInfo)GetObjectFromDataRow(row);
                    religions.Add(objReligionsInfo);
                }
            }
            ds.Dispose();
            return religions;
        }
    }
    #endregion
}