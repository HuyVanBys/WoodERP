using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region HRConfigScales
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:HRConfigScalesController
    //Created Date:Monday, May 6, 2019
    //-----------------------------------------------------------

    public class HRConfigScalesController : BaseBusinessController
    {
        public HRConfigScalesController()
        {
            dal = new DALBaseProvider("HRConfigScales", typeof(HRConfigScalesInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<HRConfigScalesInfo> entries = new List<HRConfigScalesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    HRConfigScalesInfo entry = (HRConfigScalesInfo)GetObjectFromDataRow(row);
                    entries.Add(entry);
                }
            }
            ds.Dispose();
            return entries;
        }
    }
    #endregion
}