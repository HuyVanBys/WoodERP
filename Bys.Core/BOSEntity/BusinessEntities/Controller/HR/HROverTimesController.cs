using BOSLib;
using System;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region HROverTimes
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:HROverTimesController
    //Created Date:Monday, April 25, 2011
    //-----------------------------------------------------------

    public class HROverTimesController : BaseBusinessController
    {
        public HROverTimesController()
        {
            dal = new DALBaseProvider("HROverTimes", typeof(HROverTimesInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<HROverTimesInfo> invoiceItemList = new List<HROverTimesInfo>();
            if (ds.Tables[0].Rows.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    HROverTimesInfo objinvoiceItemsInfo = (HROverTimesInfo)GetObjectFromDataRow(row);
                    invoiceItemList.Add(objinvoiceItemsInfo);
                }
            }
            ds.Dispose();
            return invoiceItemList;
        }
        public List<HROverTimesInfo> GetOverTimeByEmployeeIDAndDate(int employeeID, DateTime fromDate, DateTime toDate)
        {
            DataSet ds = dal.GetDataSet("HROverTimes_GetOverTimeByEmployeeIDAndDate", employeeID, fromDate, toDate);
            List<HROverTimesInfo> invoiceItems = new List<HROverTimesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    HROverTimesInfo objInvoiceItemsInfo = (HROverTimesInfo)GetObjectFromDataRow(row);
                    invoiceItems.Add(objInvoiceItemsInfo);
                }
            }
            ds.Dispose();
            return invoiceItems;
        }

        public List<HROverTimesInfo> GetOverTimeByEmployeeIDAndExactDate(int employeeID, DateTime getDate)
        {
            DataSet ds = dal.GetDataSet("HROverTimes_GetOverTimeByEmployeeIDAndExactDate", employeeID, getDate);
            List<HROverTimesInfo> invoiceItems = new List<HROverTimesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    HROverTimesInfo objInvoiceItemsInfo = (HROverTimesInfo)GetObjectFromDataRow(row);
                    invoiceItems.Add(objInvoiceItemsInfo);
                }
            }
            ds.Dispose();
            return invoiceItems;
        }
    }
    #endregion
}