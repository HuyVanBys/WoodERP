using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region HRBreakTimes
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:HRBreakTimesController
    //Created Date:Friday, December 05, 2014
    //-----------------------------------------------------------

    public class HRBreakTimesController : BaseBusinessController
    {
        public HRBreakTimesController()
        {
            dal = new DALBaseProvider("HRBreakTimes", typeof(HRBreakTimesInfo));
        }

        public List<HRBreakTimesInfo> GetBreakTimeList()
        {
            DataSet ds = dal.GetAllObject();
            return (List<HRBreakTimesInfo>)GetListFromDataSet(ds);
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<HRBreakTimesInfo> breakTimeList = new List<HRBreakTimesInfo>();
            if (ds.Tables[0].Rows.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    HRBreakTimesInfo breakTime = (HRBreakTimesInfo)GetObjectFromDataRow(row);
                    breakTimeList.Add(breakTime);
                }
            }
            ds.Dispose();
            return breakTimeList;
        }
    }
    #endregion
}