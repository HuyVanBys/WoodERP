using BOSLib;
using System;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region ACBusinessResults
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:ACBusinessResultsController
    //Created Date:Wednesday, March 12, 2014
    //-----------------------------------------------------------

    public class ACBusinessResultsController : BaseBusinessController
    {
        public ACBusinessResultsController()
        {
            dal = new DALBaseProvider("ACBusinessResults", typeof(ACBusinessResultsInfo));
        }

        public ACBusinessResultsInfo GetBusinessResults(DateTime? fromDate,
                                                    DateTime? toDate)
        {
            DataSet ds = dal.GetDataSet("ACBusinessResults_GetBusinessResults", fromDate, toDate);
            if (ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0)
            {
                ACBusinessResultsInfo obj = (ACBusinessResultsInfo)GetObjectFromDataRow(ds.Tables[0].Rows[0]);
                return obj;
            }
            return null;
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<ACBusinessResultsInfo> list = new List<ACBusinessResultsInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ACBusinessResultsInfo obj = (ACBusinessResultsInfo)GetObjectFromDataRow(row);
                    list.Add(obj);
                }
            }
            ds.Dispose();
            return list;
        }
    }
    #endregion
}