using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region ADInsurrancePlans
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:ADInsurrancePlansController
    //Created Date:Thursday, December 12, 2019
    //-----------------------------------------------------------

    public class ADInsurrancePlansController : BaseBusinessController
    {
        public ADInsurrancePlansController()
        {
            dal = new DALBaseProvider("ADInsurrancePlans", typeof(ADInsurrancePlansInfo));
        }

        public List<ADInsurrancePlansInfo> GetInsurrancePlanListByType(string type)
        {
            DataSet ds = dal.GetDataSet("ADInsurrancePlans_GetInsurrancePlanListByType", type);
            return (List<ADInsurrancePlansInfo>)GetListFromDataSet(ds);
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<ADInsurrancePlansInfo> list = new List<ADInsurrancePlansInfo>();
            if (ds != null && ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ADInsurrancePlansInfo obj = (ADInsurrancePlansInfo)GetObjectFromDataRow(row);
                    list.Add(obj);
                }
            }
            ds.Dispose();
            return list;
        }
    }
    #endregion
}