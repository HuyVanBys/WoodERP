using BOSLib;
using System.Collections.Generic;
using System.Data;


namespace BOSERP
{
    #region ADInsurrances
    //-----------------------------------------------------------
    //Generated By: BOS Studio
    //Class:ADInsurrancesController
    //Created Date:Thursday, March 28, 2019
    //-----------------------------------------------------------

    public class ADInsurrancesController : BaseBusinessController
    {
        public ADInsurrancesController()
        {
            dal = new DALBaseProvider("ADInsurrances", typeof(ADInsurrancesInfo));
        }

        public override System.Collections.IList GetListFromDataSet(DataSet ds)
        {
            List<ADInsurrancesInfo> list = new List<ADInsurrancesInfo>();
            if (ds.Tables.Count > 0)
            {
                foreach (DataRow row in ds.Tables[0].Rows)
                {
                    ADInsurrancesInfo objEmployeeRewardsInfo = (ADInsurrancesInfo)GetObjectFromDataRow(row);
                    list.Add(objEmployeeRewardsInfo);
                }
            }
            ds.Dispose();
            return list;
        }
    }
    #endregion
}